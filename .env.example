# Frontend: URL of the WebSocket comms relay (baked into the Next.js build)
NEXT_PUBLIC_COMM_WS_URL=ws://localhost:8001

# Backend API (FastAPI / uvicorn)
API_HOST=0.0.0.0
API_PORT=8000

# WebSocket comms relay
WS_HOST=0.0.0.0
WS_PORT=8001
